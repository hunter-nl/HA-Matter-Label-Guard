{
  "config": {
    "step": {
      "user": {
        "title": "Matter Label Guard",
        "description": "Restores a Matter node label only when the device reports an empty label. Discovered nodes are managed as separate configuration entries.",
        "data": {
          "interval_minutes": "Check interval (minutes)"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Matter Label Guard",
        "description": "{nodes} node entries · {guarded} guarded · {offline} offline · {deleted} deleted",
        "menu_options": {
          "settings": "Check interval"
        }
      },
      "settings": {
        "title": "Check interval",
        "data": {
          "interval_minutes": "Check interval (minutes)"
        }
      }
    }
  },
  "config_subentries": {
    "node_label": {
      "title": "Matter node label",
      "entry_type": "Matter node label",
      "initiate_flow": {
        "user": "Add Matter node",
        "reconfigure": "Configure Matter node"
      },
      "step": {
        "user": {
          "title": "Add Matter node",
          "data": {
            "identifier": "Matter node"
          }
        },
        "configure": {
          "title": "Configure Matter node",
          "description": "Current Matter label: {current_label}\nStatus: {status}",
          "data": {
            "label": "Label to restore",
            "guarded": "Guard this label"
          }
        },
        "reconfigure": {
          "title": "Matter node label",
          "menu_options": {
            "settings": "Label settings",
            "refresh": "Refresh node status"
          }
        },
        "settings": {
          "title": "Matter node label",
          "description": "Current Matter label: {current_label}\nStatus: {status}",
          "data": {
            "label": "Label to restore",
            "guarded": "Guard this label"
          }
        }
      },
      "abort": {
        "no_nodes": "All currently discovered Matter nodes already have entries.",
        "node_not_found": "This Matter node is no longer retained by Matter Server.",
        "matter_unavailable": "The Matter integration is not currently loaded.",
        "refresh_successful": "Node status refreshed: {status}."
      }
    }
  }
}
