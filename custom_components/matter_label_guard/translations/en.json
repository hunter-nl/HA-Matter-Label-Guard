{
  "config": {
    "step": {
      "user": {
        "title": "Matter Label Guard",
        "description": "Restores a Matter node label only when the device reports an empty label. You can manage labels within the configuration.",
        "data": {
          "interval_minutes": "Check interval (minutes)"
        }
      }
    },
    "error": {
      "invalid_identifier": "Use the Matter Server node identifier format, for example @1:2a.",
      "identifier_exists": "This Matter node is already in the label list. Choose Edit label instead.",
      "invalid_label": "Enter a label."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Matter Label Guard",
        "description": "Configured node labels:\n{labels}\n\nManage your Matter node labels or the check interval. The integration reloads after saving.",
        "menu_options": {
          "settings": "Check interval",
          "add_label": "Add a node label",
          "edit_label": "Edit a node label",
          "remove_label": "Remove a node label",
          "reset_labels": "Restore the supplied label list"
        }
      },
      "settings": {
        "title": "Check interval",
        "data": {
          "interval_minutes": "Check interval (minutes)"
        }
      },
      "add_label": {
        "title": "Add a node label",
        "data": {
          "identifier": "Matter node identifier",
          "label": "Label"
        }
      },
      "edit_label": {
        "title": "Choose a node label",
        "data": {
          "identifier": "Matter node identifier"
        }
      },
      "edit_value": {
        "title": "Edit {identifier}",
        "data": {
          "label": "Label"
        }
      },
      "remove_label": {
        "title": "Remove a node label",
        "description": "Removing this entry stops Matter Label Guard from restoring its label.",
        "data": {
          "identifier": "Matter node identifier"
        }
      },
      "reset_labels": {
        "title": "Restore the supplied label list",
        "description": "This discards the current list and restores the supplied labels."
      }
    },
    "error": {
      "invalid_identifier": "Use the Matter Server identifier format, for example @1:2a.",
      "identifier_exists": "This Matter node is already in the label list. Choose Edit label instead.",
      "invalid_label": "Enter a label."
    }
  }
}
